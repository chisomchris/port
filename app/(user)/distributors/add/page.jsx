import { Main } from '../../../components/Main/Main'

export const metadata = {
    title: "Add Distributor | Dashboard",
};

export default function DashBoard() {
    return (
        <Main title={'Add Distributor'}>
            <p>Add Distributor</p>
        </Main>
    )
}
